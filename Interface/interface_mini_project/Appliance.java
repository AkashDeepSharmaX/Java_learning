package interface_mini_project;

public interface Appliance {
    void on();
    // abstract method implementing classes must define this method
    // No need to write access modifiers like public or abstract,

    void off();
    // abstract method implementing classes must define this method
}
